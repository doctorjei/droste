# OCI seed exclusions: packages from lxc/seed-target.txt that are excluded
# from the OCI base image because they cannot run or serve no purpose in
# standard OCI containers (no init system, no kernel access, no cloud
# metadata endpoint).
#
# Libraries (libsystemd0, libdbus-1-3, libapparmor1, etc.) are NOT excluded —
# they install harmlessly and are often needed as deps of other packages.
#
# The OCI base is defined as: seed-target.txt MINUS this file.
# This keeps OCI in sync with LXC seed automatically.
#
# Generated: 2026-03-04

# Init system (OCI containers don't run systemd as PID 1)
init
systemd
systemd-resolved
systemd-sysv
systemd-timesyncd
libpam-systemd
libsystemd-shared

# Device/kernel management (no direct kernel access in OCI)
udev
kmod
dmsetup
apparmor

# D-Bus system bus (needs init system to function)
dbus
dbus-bin
dbus-daemon
dbus-session-bus-common
dbus-system-bus-common

# Authorization daemon (needs D-Bus)
polkitd

# Cloud metadata services (no cloud endpoint in OCI)
cloud-init
cloud-guest-utils
cloud-image-utils
cloud-utils
ssh-import-id

# SSH server (not standard practice for OCI containers)
openssh-server
openssh-sftp-server

# Automatic updates (container images are immutable)
unattended-upgrades

# VM-specific tools
qemu-utils
