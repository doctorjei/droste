name: Build droste tier
description: >
  Log in to GHCR, set up Buildx, and build+push a single droste tier image.
  Optionally passes a BASE_IMAGE build-arg pointing at a parent tier pinned
  by the current commit SHA. Caches per-tier via GitHub Actions cache.

inputs:
  tier:
    description: Tier name (e.g. seed, fiber, wool). Used for Containerfile path and image name.
    required: true
  base-tier:
    description: >
      Parent tier name (e.g. seed). Leave empty for tiers with no parent
      (only droste-seed — it builds FROM canopy, not another droste tier).
    required: false
    default: ''

runs:
  using: composite
  steps:
    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ github.token }}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build and push ${{ inputs.tier }}
      uses: docker/build-push-action@v6
      with:
        context: .
        file: containers/droste-${{ inputs.tier }}/Containerfile
        push: true
        tags: |
          ghcr.io/${{ github.repository_owner }}/droste-${{ inputs.tier }}:latest
          ghcr.io/${{ github.repository_owner }}/droste-${{ inputs.tier }}:${{ github.sha }}
        build-args: ${{ inputs.base-tier != '' && format('BASE_IMAGE=ghcr.io/{0}/droste-{1}:{2}', github.repository_owner, inputs.base-tier, github.sha) || '' }}
        # Cache scope folds in a hash of every build input that can change
        # image content: tier Containerfiles, seed's canopy-restore.txt,
        # and oci/**. Without this, a change to a parent tier's Containerfile
        # (e.g., a new RUN in lint) could leave downstream scopes pointing at
        # layer blobs predating the change — BuildKit reuses them and the
        # new instruction never executes. Keying on content hash forces a
        # fresh scope whenever any input changes, invalidating all tiers'
        # caches together. Check files aren't hashed; they don't affect
        # image content.
        cache-from: type=gha,scope=${{ inputs.tier }}-${{ hashFiles('containers/droste-*/Containerfile', 'containers/droste-*/*.txt', 'oci/**') }}
        cache-to: type=gha,mode=max,scope=${{ inputs.tier }}-${{ hashFiles('containers/droste-*/Containerfile', 'containers/droste-*/*.txt', 'oci/**') }}
